const config = {
  semi: true, // Añade punto y coma al final de las sentencias
  trailingComma: 'es5', // Añade coma final en objetos y arrays cuando sea compatible con ES5
  singleQuote: true, // Usa comillas simples en lugar de comillas dobles
  printWidth: 80, // Máximo de caracteres por línea
  tabWidth: 2, // Usa 2 espacios por nivel de indentación
  useTabs: true, // Usa espacios en lugar de tabs
  jsxSingleQuote: false, // Usa comillas dobles en JSX
  arrowParens: 'avoid', // Evita los paréntesis en funciones de un solo parámetro
  bracketSpacing: true, // Añade espacio entre llaves y el contenido
};

export default config;